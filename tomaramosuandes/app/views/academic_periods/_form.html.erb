<%= form_with(model: academic_period) do |form| %>
  <% if academic_period.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(academic_period.errors.count, "error") %> prohibited this academic_period from being saved:</h2>

      <ul>
        <% academic_period.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
