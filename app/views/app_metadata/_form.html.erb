<%= form_with(model: app_metadatum) do |form| %>
  <% if app_metadatum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(app_metadatum.errors.count, "error") %> prohibited this app_metadatum from being saved:</h2>

      <ul>
        <% app_metadatum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :latest_version_name, style: "display: block" %>
    <%= form.text_field :latest_version_name %>
  </div>

  <div>
    <%= form.label :catalog_current_period, style: "display: block" %>
    <%= form.text_field :catalog_current_period %>
  </div>

  <div>
    <%= form.label :catalog_last_updated, style: "display: block" %>
    <%= form.text_field :catalog_last_updated %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
