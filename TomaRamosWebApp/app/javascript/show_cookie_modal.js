/**
 * When imported, displays the cookie consent dialog, so backend login in templates must decide
 * whether to import this JS file.
 */

document.addEventListener("DOMContentLoaded", function() {
    document.getElementById("cookieButton").click()
});