# Database connection settings.

default: &default
  adapter: postgresql
  encoding: unicode
  #// encoding: utf8
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>


development:
  <<: *default
  url: "postgresql://tomaramosuandes:tomaramosuandes@localhost:<%= ENV.fetch('POSTGRES_PORT') { 5432 } %>/tomaramosuandes_development"

  # Schema search path. The server defaults to $user,public
  #schema_search_path: myapp,sharedapp,public

  # Minimum log levels, in increasing order:
  #   debug5, debug4, debug3, debug2, debug1,
  #   log, notice, warning, error, fatal, and panic
  # Defaults to warning.
  min_messages: notice


# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  url: "postgresql://tomaramosuandes:tomaramosuandes@localhost:<%= ENV.fetch('POSTGRES_PORT') { 5432 } %>/tomaramosuandes_test"


production:
  <<: *default
  url: "postgresql://tomaramosuandes:<%= ENV.fetch('TOMARAMOSUANDES_DATABASE_PASSWORD') { 'foo' } %>@localhost:<%= ENV.fetch('POSTGRES_PORT') { 5432 } %>/tomaramosuandes_production"
